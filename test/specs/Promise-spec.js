define(['Promise'], function(Promise) {

    'use strict';

    describe('Promise', function() {

        it('exists', function() {
            expect(typeof Promise).toBe('function');
        });

    });

});
